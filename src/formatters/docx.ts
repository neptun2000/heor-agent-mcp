export function resultsToDocx(_results: unknown, _audit: unknown): string {
  return "⚠️ DOCX export is available in the hosted tier. Upgrade at https://heor-agent.io to enable report generation.";
}
